import {ResponsiveContainer,AreaChart,Area,CartesianGrid,XAxis,YAxis,Tooltip,BarChart,Bar} from 'recharts'
export function TrendChart({data,dataKey='value',xKey='name',kind='area'}:{data:any[];dataKey?:string;xKey?:string;kind?:'area'|'bar'}){if(!data.length)return <div className="flex h-72 items-center justify-center text-sm text-zinc-400">No chart data for this period.</div>;return <div className="h-72 w-full"><ResponsiveContainer>{kind==='bar'?<BarChart data={data}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey={xKey} tick={{fontSize:11}}/><YAxis tick={{fontSize:11}}/><Tooltip/><Bar dataKey={dataKey} fill="#b4236f" radius={[6,6,0,0]}/></BarChart>:<AreaChart data={data}><defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#b4236f" stopOpacity={.24}/><stop offset="95%" stopColor="#b4236f" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey={xKey} tick={{fontSize:11}}/><YAxis tick={{fontSize:11}}/><Tooltip/><Area type="monotone" dataKey={dataKey} stroke="#b4236f" strokeWidth={2.2} fill="url(#fill)"/></AreaChart>}</ResponsiveContainer></div>}
