import {ChevronLeft,ChevronRight} from 'lucide-react'
export function Pagination({count,page,pageSize=20,onPage}:{count:number;page:number;pageSize?:number;onPage:(p:number)=>void}){const pages=Math.max(1,Math.ceil(count/pageSize));if(pages<=1)return null;return <div className="flex items-center justify-between border-t border-zinc-100 px-4 py-3 text-sm"><span className="text-zinc-500">Page {page} of {pages} · {count} records</span><div className="flex gap-2"><button className="btn-secondary px-3" disabled={page<=1} onClick={()=>onPage(page-1)}><ChevronLeft size={16}/></button><button className="btn-secondary px-3" disabled={page>=pages} onClick={()=>onPage(page+1)}><ChevronRight size={16}/></button></div></div>}
