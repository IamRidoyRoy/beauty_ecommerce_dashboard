import type {LucideIcon} from 'lucide-react'
export function StatCard({label,value,sub,icon:Icon,tone='neutral',interactive=false}:{label:string;value:string|number;sub?:string;icon:LucideIcon;tone?:'neutral'|'success'|'warning'|'danger';interactive?:boolean}){const tones={neutral:'bg-zinc-100 text-zinc-700',success:'bg-emerald-50 text-emerald-700',warning:'bg-amber-50 text-amber-700',danger:'bg-red-50 text-red-700'};return <div className={`panel h-full p-5 transition duration-200 ${interactive?'cursor-pointer group-hover:-translate-y-0.5 group-hover:border-pink-200 group-hover:shadow-md':''}`}><div className="flex items-start justify-between"><div><p className={`text-sm font-medium transition ${interactive?'text-zinc-500 group-hover:text-pink-700':'text-zinc-500'}`}>{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-zinc-950">{value}</p>{sub&&<p className="mt-1 text-xs text-zinc-500">{sub}</p>}</div><div className={`rounded-xl p-2.5 transition ${tones[tone]} ${interactive?'group-hover:scale-105':''}`}><Icon size={19}/></div></div></div>}
