import {Modal} from './Modal'
export function ConfirmDialog({open,onClose,onConfirm,title='Confirm action',message='This action cannot be undone.',danger=false,busy=false}:{open:boolean;onClose:()=>void;onConfirm:()=>void;title?:string;message?:string;danger?:boolean;busy?:boolean}){return <Modal open={open} onClose={onClose} title={title} size="sm"><p className="text-sm leading-6 text-zinc-600">{message}</p><div className="mt-6 flex justify-end gap-2"><button className="btn-secondary" onClick={onClose}>Cancel</button><button className={danger?'btn-danger':'btn-primary'} onClick={onConfirm} disabled={busy}>{busy?'Working…':'Confirm'}</button></div></Modal>}
