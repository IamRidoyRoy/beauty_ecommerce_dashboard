import {Inbox} from 'lucide-react';import type {ReactNode} from 'react'
export function EmptyState({title='Nothing here yet',description='No records matched your current view.',action}:{title?:string;description?:string;action?:ReactNode}){return <div className="panel flex min-h-64 flex-col items-center justify-center px-6 py-12 text-center"><div className="mb-4 rounded-2xl bg-zinc-100 p-3 text-zinc-500"><Inbox/></div><h3 className="font-semibold text-zinc-900">{title}</h3><p className="mt-1 max-w-md text-sm text-zinc-500">{description}</p>{action&&<div className="mt-4">{action}</div>}</div>}
