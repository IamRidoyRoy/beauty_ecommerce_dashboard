import {useState} from 'react';import {useParams} from 'react-router';import {Printer,PackageCheck,XCircle,CheckCircle2} from 'lucide-react';import {PageHeader} from '../../components/ui/PageHeader';import {StatusBadge} from '../../components/ui/StatusBadge';import {PageLoading} from '../../components/ui/Loading';import {Modal} from '../../components/ui/Modal';import {usePurchaseQuery,useApprovePurchaseMutation,useCancelPurchaseMutation,useReceivePurchaseMutation,useSuppliersQuery} from '../../services/purchaseApi';import {useWarehousesQuery} from '../../services/inventoryApi';import {money,date} from '../../utils/format';import {rowsOf,apiError} from '../../utils/data';import {useAppDispatch} from '../../store/hooks';import {toast} from '../../features/ui/uiSlice'
export default function PurchaseDetailPage(){const {id}=useParams(),q=usePurchaseQuery(Number(id)),ss=useSuppliersQuery(),ws=useWarehousesQuery(),[approve,{isLoading:approving}]=useApprovePurchaseMutation(),[cancel]=useCancelPurchaseMutation(),[receive,{isLoading:receiving}]=useReceivePurchaseMutation(),dispatch=useAppDispatch(),[open,setOpen]=useState(false),[receipts,setReceipts]=useState<Record<number,number>>({});if(q.isLoading||!q.data)return <PageLoading/>;const p=q.data,supplier=rowsOf<any>(ss.data).find(x=>x.id===p.supplier),wh=rowsOf<any>(ws.data).find(x=>x.id===p.warehouse);const act=async(fn:()=>Promise<any>,msg:string)=>{try{await fn();dispatch(toast({type:'success',message:msg}))}catch(e){dispatch(toast({type:'error',message:apiError(e)}))}};return <><PageHeader title={p.purchase_number} description={`${supplier?.name||'Supplier'} → ${wh?.name||'Warehouse'}`} actions={<><button className="btn-secondary" onClick={()=>window.print()}><Printer size={16}/>Print Purchase</button>{p.status==='draft'&&<button className="btn-brand" disabled={approving} onClick={()=>act(()=>approve(p.id).unwrap(),'Purchase ordered; incoming stock recorded.')}><CheckCircle2 size={16}/>Order Purchase</button>}{['approved','partial'].includes(p.status)&&<button className="btn-brand" onClick={()=>{setReceipts(Object.fromEntries(p.items.filter(i=>i.remaining_quantity>0).map(i=>[i.id,i.remaining_quantity])));setOpen(true)}}><PackageCheck size={16}/>Receive</button>}{['draft','approved'].includes(p.status)&&<button className="btn-secondary text-red-600" onClick={()=>act(()=>cancel(p.id).unwrap(),'Purchase cancelled.')}><XCircle size={16}/>Cancel</button>}</>}/><div className="grid gap-5 lg:grid-cols-3"><section className="panel p-5 lg:col-span-2"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">Purchase Items</h2><StatusBadge value={p.status==='approved'?'ordered':p.status}/></div><div className="overflow-x-auto"><table className="table"><thead><tr><th>Target</th><th>Ordered</th><th>Received</th><th>Remaining</th><th>Unit Cost</th><th>Total</th></tr></thead><tbody>{p.items.map(i=><tr key={i.id}><td className="font-medium">{i.product?`Product #${i.product}`:`Variant #${i.product_variant}`}</td><td>{i.quantity}</td><td>{i.received_quantity}</td><td>{i.remaining_quantity}</td><td>{money(i.unit_cost)}</td><td>{money(i.total)}</td></tr>)}</tbody></table></div></section><aside className="panel p-5"><h2 className="font-semibold">Purchase Summary</h2><dl className="mt-4 space-y-3 text-sm">{[['Supplier',supplier?.name],['Warehouse',wh?.name],['Supplier Invoice',p.supplier_invoice||'—'],['Purchase Date',date(p.purchase_date)],['Expected',date(p.expected_date)],['Subtotal',money(p.subtotal)],['Discount',money(p.discount)],['Tax',money(p.tax)]].map(([a,b])=><div key={a} className="flex justify-between gap-4"><dt className="text-zinc-400">{a}</dt><dd className="text-right font-medium">{b}</dd></div>)}<div className="flex justify-between border-t border-zinc-200 pt-3 text-base"><dt className="font-semibold">Total</dt><dd className="font-bold">{money(p.total)}</dd></div></dl></aside></div><Modal open={open} onClose={()=>setOpen(false)} title="Receive Purchase"><p className="mb-4 text-sm text-zinc-500">Enter only quantities physically received now. The backend locks remaining quantities and prevents double-stock.</p><div className="space-y-3">{p.items.filter(i=>i.remaining_quantity>0).map(i=><label key={i.id} className="grid grid-cols-[1fr_120px] items-center gap-3 rounded-xl border border-zinc-200 p-3"><span className="text-sm"><b>{i.product?`Product #${i.product}`:`Variant #${i.product_variant}`}</b><small className="block text-zinc-400">Remaining {i.remaining_quantity}</small></span><input className="input" type="number" min="0" max={i.remaining_quantity} value={receipts[i.id]??0} onChange={e=>setReceipts({...receipts,[i.id]:Number(e.target.value)})}/></label>)}</div><button className="btn-brand mt-5 w-full" disabled={receiving} onClick={()=>act(async()=>{await receive({id:p.id,receipts:Object.entries(receipts).filter(([,qty])=>qty>0).map(([item_id,quantity])=>({item_id:Number(item_id),quantity}))}).unwrap();setOpen(false)},'Purchase quantities received.')}>{receiving?'Receiving…':'Confirm Receive'}</button></Modal></>}
