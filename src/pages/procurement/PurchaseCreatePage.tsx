import {useMemo,useState} from 'react';import {Plus,Trash2} from 'lucide-react';import {useNavigate} from 'react-router';import {PageHeader} from '../../components/ui/PageHeader';import {Field,Input,Select} from '../../components/forms/FormField';import {useSuppliersQuery,useCreatePurchaseMutation,useCreatePurchaseItemMutation} from '../../services/purchaseApi';import {useWarehousesQuery} from '../../services/inventoryApi';import {useProductsQuery,useVariantsQuery} from '../../services/catalogApi';import {rowsOf,apiError} from '../../utils/data';import {money} from '../../utils/format';import {useAppDispatch} from '../../store/hooks';import {toast} from '../../features/ui/uiSlice'
type Row={product:string;variant:string;quantity:string;unit_cost:string;discount:string;tax:string}
const blank=():Row=>({product:'',variant:'',quantity:'1',unit_cost:'0',discount:'0',tax:'0'})
export default function PurchaseCreatePage(){const nav=useNavigate(),dispatch=useAppDispatch(),ss=useSuppliersQuery(),ws=useWarehousesQuery(),ps=useProductsQuery({page_size:500}),vs=useVariantsQuery({page_size:1000}),[createPurchase,{isLoading}]=useCreatePurchaseMutation(),[createItem]=useCreatePurchaseItemMutation(),[head,setHead]=useState({supplier:'',warehouse:'',supplier_invoice:'',purchase_date:new Date().toISOString().slice(0,10),expected_date:'',note:''}),[items,setItems]=useState<Row[]>([blank()]);const products=rowsOf<any>(ps.data),variants=rowsOf<any>(vs.data);const totals=useMemo(()=>items.reduce((a,r)=>{const line=Number(r.quantity||0)*Number(r.unit_cost||0)-Number(r.discount||0)+Number(r.tax||0);return a+line},0),[items]);const save=async()=>{try{if(!head.supplier||!head.warehouse)throw new Error('Supplier and warehouse are required.');if(items.some(r=>!r.product))throw new Error('Every purchase row needs a product.');const n=`PO-${new Date().toISOString().replace(/[-:TZ.]/g,'').slice(0,14)}`;const purchase=await createPurchase({purchase_number:n,supplier:Number(head.supplier),warehouse:Number(head.warehouse),supplier_invoice:head.supplier_invoice,purchase_date:head.purchase_date,expected_date:head.expected_date||null,subtotal:totals,discount:0,tax:items.reduce((a,r)=>a+Number(r.tax||0),0),total:totals}).unwrap();for(const r of items){const p=products.find(x=>x.id===Number(r.product));if(!p)continue;const line=Number(r.quantity)*Number(r.unit_cost)-Number(r.discount||0)+Number(r.tax||0);await createItem({purchase:purchase.id,product:p.product_type==='simple'?p.id:null,product_variant:p.product_type==='variable'?Number(r.variant):null,quantity:Number(r.quantity),unit_cost:r.unit_cost,discount:r.discount||0,tax:r.tax||0,total:line}).unwrap()}dispatch(toast({type:'success',message:'Purchase draft created.'}));nav(`/procurement/purchases/${purchase.id}`)}catch(e){dispatch(toast({type:'error',message:e instanceof Error?e.message:apiError(e)}))}};return <><PageHeader title="Create Purchase" description="Simple products are purchased directly; variable products require their real variant."/><div className="panel p-6"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Field label="Supplier"><Select value={head.supplier} onChange={e=>setHead({...head,supplier:e.target.value})}><option value="">Select supplier</option>{rowsOf<any>(ss.data).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</Select></Field><Field label="Warehouse"><Select value={head.warehouse} onChange={e=>setHead({...head,warehouse:e.target.value})}><option value="">Select warehouse</option>{rowsOf<any>(ws.data).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</Select></Field><Field label="Supplier Invoice"><Input value={head.supplier_invoice} onChange={e=>setHead({...head,supplier_invoice:e.target.value})}/></Field><Field label="Purchase Date"><Input type="date" value={head.purchase_date} onChange={e=>setHead({...head,purchase_date:e.target.value})}/></Field><Field label="Expected Date"><Input type="date" value={head.expected_date} onChange={e=>setHead({...head,expected_date:e.target.value})}/></Field></div><div className="mt-7 flex items-center justify-between"><h2 className="font-semibold">Purchase Items</h2><button className="btn-secondary" onClick={()=>setItems([...items,blank()])}><Plus size={15}/>Add item</button></div><div className="mt-3 overflow-x-auto rounded-2xl border border-zinc-200"><table className="table"><thead><tr><th>Product</th><th>Variant</th><th>Qty</th><th>Unit Cost</th><th>Discount</th><th>Tax</th><th>Total</th><th/></tr></thead><tbody>{items.map((r,i)=>{const p=products.find(x=>x.id===Number(r.product)),line=Number(r.quantity||0)*Number(r.unit_cost||0)-Number(r.discount||0)+Number(r.tax||0);return <tr key={i}><td><select className="input min-w-60" value={r.product} onChange={e=>setItems(items.map((x,j)=>j===i?{...x,product:e.target.value,variant:''}:x))}><option value="">Select</option>{products.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></td><td>{p?.product_type==='variable'?<select className="input min-w-44" value={r.variant} onChange={e=>setItems(items.map((x,j)=>j===i?{...x,variant:e.target.value}:x))}><option value="">Variant</option>{variants.filter(v=>v.product===p.id).map(v=><option key={v.id} value={v.id}>{v.sku}</option>)}</select>:<span className="text-xs text-zinc-400">Not required</span>}</td>{(['quantity','unit_cost','discount','tax'] as const).map(k=><td key={k}><input type="number" min="0" className="input min-w-24" value={r[k]} onChange={e=>setItems(items.map((x,j)=>j===i?{...x,[k]:e.target.value}:x))}/></td>)}<td className="font-semibold">{money(line)}</td><td><button disabled={items.length===1} onClick={()=>setItems(items.filter((_,j)=>j!==i))} className="rounded-lg p-2 text-red-500 hover:bg-red-50"><Trash2 size={16}/></button></td></tr>})}</tbody></table></div><div className="mt-5 flex justify-end"><div className="text-right"><div className="text-sm text-zinc-500">Purchase Total</div><div className="text-2xl font-bold">{money(totals)}</div></div></div><div className="mt-6 flex justify-end gap-2"><button className="btn-secondary" onClick={()=>nav('/procurement/purchases')}>Cancel</button><button disabled={isLoading} onClick={save} className="btn-brand">{isLoading?'Creating…':'Create Purchase Draft'}</button></div></div></>}
