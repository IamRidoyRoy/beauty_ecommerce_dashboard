# Purchase UI update

- Purchase Create product selector is searchable by product name and SKU.
- Variable product variants are searchable by SKU and attribute labels.
- Search dropdowns render above table overflow containers.
- Purchase Detail displays target image, name, SKU and variant attributes.
- Receive Purchase modal uses the same target details.
