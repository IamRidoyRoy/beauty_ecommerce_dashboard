# Review Page Fix

- Fixed ReviewsPage importing `useNavigate` from `react-router-dom` even though the project uses `react-router`.
- Reviews page now defaults to All statuses instead of Pending only.
- No backend/database migration is required.
